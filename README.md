Lala AI Starter v2 (AWS CDK / TypeScript)
- Stable deps, no alpha packages, CloudFront OAI, Cognito Hosted UI, AppSync + Lambda.

Install:
  npm install
Build:
  npm run build
Deploy:
  npm run deploy
